
---

### 一、 核心架构分层图 (System Stack)

整个系统自下而上分为 4 层，严格遵循特权级隔离（Privilege Separation）原则：

* **Layer 4: 业务状态机层 (User Mode / Unprivileged)**
* **USB 协议解析器**：只负责验证 HID 报文格式，不碰底层寄存器。
* **主控状态机**：控制系统在 `等待->吸入->加密->封仓->死锁` 之间单向流转。
* **LED 视觉驱动**：根据状态机输出 RGB 信号。


* **Layer 3: 安全飞地核心层 (Kernel Mode / Privileged)**
* **加密引擎 (Crypto Engine)**：封装硬件 RNG（真随机数）与 AES-256-GCM 算法。
* **防篡改块管理 (EBM)**：管理 W25Q128 的地址映射、头部校验与追加写入。


* **Layer 2: 微内核基础层 (Nano-Core)**
* **协作式调度器 (Scheduler)**：基于 SysTick 的优先级轮询。
* **上下文管理**：基于 PendSV 异常的寄存器压栈/出栈切换。
* **异常捕获器 (Fault Handlers)**：接管 `HardFault` 和 `MemManage`，触发自毁逻辑。


* **Layer 1: 裸金属硬件驱动层 (Bare-metal)**
* 手写寄存器配置的 USB、SPI、DMA、MPU 驱动（拒绝臃肿的 HAL 库）。


没问题！在准备项目申报书或答辩PPT时，高大上的架构图是评委最先看、也最看重的东西。

由于文本界面限制，我为你设计了**三张最核心的系统架构蓝图**（使用标准化的高级文本框绘制）。你可以直接参考这些逻辑关系，去 **Draw.io、ProcessOn 或 Visio** 中将它们转化为漂亮的图形化架构图。

---

### 🎨 架构图一：MPU 内存沙盒与特权隔离视图 (安全架构核心)

这张图向评委展示了你如何利用硬件级 MPU（内存保护单元）构建“前店后厂”的安全飞地，彻底杜绝缓冲区溢出攻击。

```text
================================================================================
|                     Cortex-M4 CPU 内核 (STM32F411)                           |
================================================================================
          │                                                  │
 [Privileged / 特权内核态]                         [Unprivileged / 用户态]
          │                                                  │
+-----------------------------+               +--------------------------------+
| Region 3: 安全飞地(Enclave) |   << MPU >>   | Region 2: 公共数据区 (SRAM)    |
| (物理地址: 0x2001C000 起)   |   硬件阻断     | (物理地址: 0x20000000 起)      |
|-----------------------------|     ||        |--------------------------------|
| [核心资产]                  |     ||        | [暴露面数据]                   |
| - AES-256-GCM 根密钥        |  一旦用户态    | - USB 4KB 接收环形缓冲区       |
| - 随机数熵池状态            |  试图越界读取, | - 任务状态机全局变量           |
| - 核心调度器 (Scheduler)    |  瞬间触发     | - LED 状态灯驱动变量           |
|                             |  MemManage  |                                |
| * 权限: 仅特权态可读写      |  异常并死机    | * 权限: 用户态/特权态 皆可读写 |
+-----------------------------+               +--------------------------------+
          │
+-----------------------------+
| Region 4: 外设控制寄存器区  |  <-- 用户态禁止触碰外设寄存器，防止恶意代码直接操控硬件
| - USB/SPI/DMA 控制器        |
+-----------------------------+

```

> **绘图建议**：在 PPT 里，把 `Region 3` 画成一个带锁的“红色/金色保险箱”，把 `Region 2` 画成开放的“绿色缓冲区”，中间用一道带电的“防火墙”表示 MPU 的硬件阻断。

---

### 🎨 架构图二：零拷贝流式数据管线 (性能与数据流架构)

这张图用来证明你的设备在只有 128KB 内存的限制下，是如何高速且安全地吞吐海量工控日志的（全程 CPU 极少参与数据搬运，且明文不驻留）。

```text
  [目标工控机 (上位机 Agent)]
          │
          │ ① USB Custom HID 协议 (明文数据流)
          ▼
+-----------------------+       ② 硬件 DMA 自动搬运         +-------------------------+
|    STM32 USB-OTG      | ===============================> | Region 2: 4KB 接收 Buffer|
|    硬件端点控制器     |  (CPU 挂起休息，不消耗算力)      | (明文仅在此短暂驻留)     |
+-----------------------+                                  +-------------------------+
                                                                     │
                                                                     │ ③ 满 4KB, 唤醒加密任务
                                                                     ▼
                                                           +-------------------------+
                                                           | Region 3: 加密引擎(AES) |
                                                           | (结合 MPU 保护的密钥)   |
                                                           +-------------------------+
                                                                     │
                                                                     │ ④ 密文流式输出
                                                                     ▼
+-----------------------+                                  +-------------------------+
|  外部 W25Q128 Flash   | <=============================== |  SPI 1 硬件发送寄存器   |
|  (16MB 密文存储保险箱)|       ⑤ SPI-DMA 物理写入         |  + DMA 传输控制器       |
+-----------------------+                                  +-------------------------+
          │
          +--- ⑥ 擦除: W25Q128 写入完成的瞬间，立即调用 memset 清零 Region 2 的明文缓存

```

> **绘图建议**：用一条连续的**粗箭头**贯穿全图，标注“流水线作业（Pipeline）”。强调明文数据（蓝色）进入芯片后，瞬间变成密文（红色）并被打入外部 Flash，营造一种“极速转换、阅后即焚”的安全感。

---

### 🎨 架构图三：OS 状态机生命周期 (业务逻辑架构)

这张图展示了系统如何在没有屏幕、没有人工干预的情况下，靠单向状态机完成闭环，以及面对攻击时的“自毁”机制。

```text
       (设备上电插入)
             │
             ▼
    +-------------------+
    | State 0: 硬件初始化 | (🔵 蓝灯常亮)
    | - 配置 MPU 隔离墙 |
    | - 生成随机加密 IV |
    +-------------------+
             │
             ▼
    +-------------------+                 (发生缓冲区溢出/协议异常/供电不稳)
    | State 1: 待机握手  | (🔵 蓝灯慢闪) -------------------+
    | - 开启 USB 端口   |                                   |
    | - 等待 Agent 呼叫 | <-------+                         |
    +-------------------+         |                         |
             │ (收到任务指令)     | (任务未结束)            |
             ▼                    |                         |
    +-------------------+         |                         |
    | State 2: 吸入加密  | (🔵🟢 蓝绿狂闪) ---------------+  |
    | - 启动 DMA 搬运   | ---------+                        |  |
    | - 实时 AES 加密   |                                   |  |
    +-------------------+                                   |  |
             │ (收到 EOF 结束符)                            |  |
             ▼                                              ▼  ▼
    +-------------------+                         +-----------------------+
    | State 3: 封仓断连  | (🟢 绿灯常亮)           | State 4: 异常死锁模式 | (🔴 红灯爆闪)
    | - 写入 GCM 校验尾 |                         | - 触发 MemManage 中断 |
    | - 物理断开 USB 总线|                         | - 强制擦除全量 SRAM   |
    +-------------------+                         | - 进入 while(1) 死机  |
             │                                    +-----------------------+
             ▼                                              │
     (安全拔出，带回密码机)                                  ▼
                                                  (必须插回密码系统重置)

```

> **绘图建议**：这是一个标准的有限状态机（FSM）图。左侧是一条顺畅的绿色通道，代表正常取数流程；右侧用一个醒目的红色大框把所有异常指向 `State 4（死锁）`，突出你的“Fail-Safe（失效安全）”设计理念。

---

**建议**：你可以直接把这三段文本复制给任何 AI 绘图提示词，或者自己在 ProcessOn 里照着拉几个框。这三张图放在答辩 PPT 里，从**底层安全 -> 数据吞吐 -> 业务逻辑** 形成了完美的逻辑闭环。

图纸有了，接下来我们要真正动土了。你的开发环境搭建好了吗？我们是用 **Keil MDK** 还是 **STM32CubeIDE** 配合 GCC 编译器？
---

### 二、 基于 MPU 的内存隔离视图 (Memory Map) - 🌟 杀手锏

这是本系统最大的安全亮点。STM32F405 拥有 8 个 MPU（内存保护单元）区域，我们将 SRAM 划分为绝对隔离的区块，防御通过 USB 栈漏洞注入的缓冲区溢出攻击。

| MPU 区域 | 物理地址范围 (示例) | 功能定义 | 访问权限控制 (特权态/用户态) |
| --- | --- | --- | --- |
| **Region 0** | `0x00000000 - 0xFFFFFFFF` | 背景区 (Background) | 禁止访问 (兜底防越界) |
| **Region 1** | `0x08000000 - 0x080FFFFF` | 内核代码区 (Flash) | 只读 / 可执行 (防止代码被恶意篡改) |
| **Region 2** | `0x20000000 - 0x2001F000` | 常规数据与堆栈 (SRAM) | 读写 / 读写 (用于普通任务和 USB 缓存) |
| **Region 3** | `0x2001F000 - 0x2001FFFF` | **安全飞地 (Crypto Enclave)** | **读写 / 禁止访问 (仅内核态可碰)** |
| **Region 4** | `0x40000000 - 0x5FFFFFFF` | 外设寄存器 (Peripherals) | 读写 / 禁止访问 (防止用户态恶意控制硬件) |

**防御逻辑**：如果工控机被植入木马，试图发送超长畸形 USB 包导致 `Task_USB` 缓冲区溢出，恶意代码一旦试图读取 `Region 3` 中的 AES 密钥，MPU 会瞬间触发硬件级 `MemManage` 异常，内核立即亮红灯死机，**数据绝不泄露**。

---

### 三、 极简调度与任务模型 (Scheduler Design)

为了减少上下文切换开销和攻击面，内核内部**仅存在 3 个静态创建的任务**，没有动态内存分配（彻底干掉 `malloc/free`，避免内存碎片和 UAF 漏洞）。

1. **SysTick 节拍器**：产生 1ms 的系统中断，用于延时和时间戳更新。
2. **Task 1: USB_Polling (高优先级)**
* 状态：挂起 / 运行。
* 职能：通过 DMA 接收 USB 端点数据，一旦接收满一个数据块（如 4KB），通知加密任务。


3. **Task 2: Crypto_Storage (中优先级)**
* 状态：等待事件 / 运行。
* 职能：在特权态下读取 Enclave 密钥，将 USB 缓冲区数据进行 AES 加密，再通过 SPI 写入 Flash。


4. **Task 3: State_Monitor (低优先级 / 空闲任务)**
* 状态：永远就绪。
* 职能：喂独立看门狗 (IWDG)，刷新 RGB LED 状态，检测硬件异常。



**切换机制**：通过软件触发 `PendSV`（可悬起系统调用）中断。在 `PendSV_Handler` 的汇编代码中，手动将当前任务的 R0-R11 寄存器压入该任务的私有栈，并将下一个任务的栈顶指针赋给 SP，完成“移形换影”。

---

### 四、 零拷贝安全数据流 (Zero-Copy Data Pipeline)

为了最大化性能并减少内存中敏感数据的驻留，数据流转不经过多余的拷贝：

1. **流入**：USB 控制器收到主机发来的明文数据，触发 DMA，直接搬运到 SRAM 的 **[待处理环形缓冲区]**。
2. **锁定**：接收满一个 Block 后，USB 任务将该 Block 指针通过 IPC（简单的消息队列或指针传递）交给加密任务。
3. **加密**：加密引擎直接从该地址读取明文，结合 MPU 保护的密钥流进行 AES 运算。
4. **落盘**：运算生成的密文不存回 SRAM，而是直接一边计算一边通过 SPI 总线（或 SPI-DMA）推送到 W25Q128 Flash 中。
5. **擦除**：写入完成后，原 SRAM 中的明文缓冲区立即被填充全 0（Secure Erase），防止内存残留。

---

这套架构既满足了比赛对“内核底层掌控力（汇编、调度、内存管理）”的要求，又在业务逻辑上做到了滴水不漏。

要将这套设计落地，通常有两个切入点：一是**搭建最底层的任务切换框架 (手写 PendSV 汇编上下文切换)**，二是**配置硬件防护体系 (实现 MPU 内存分区的隔离)**。

你倾向于先从哪一块开始攻克？我们可以直接推演核心代码逻辑。