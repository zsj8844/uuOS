这份文档将是你项目申报书中最具含金量的“软件详细设计规格说明书（SDS）”。

为了确保整个“极简微内核”高内聚、低耦合，我们采用**严格的分层架构**。下面我为你完整梳理整个 SecuFerry-OS 的全景设计，包括完整的目录树、模块细节、依赖关系以及步步为营的测试验收标准。

---

### 📂 一、 完整工程目录树 (Project Directory Tree)

为了配合 MPU 的内存隔离（安全飞地），工程的目录结构必须与链接器脚本（Linker Script）强绑定。以下是最终的物理工程目录：

```text
SecuFerry_OS_Workspace/
 ├── 📁 Core_Boot/                  # 核心启动与链接层 (系统基石)
 │    ├── startup_stm32f411xe.s     # 汇编启动文件 (中断向量表定义)
 │    └── STM32F411CEUX_FLASH.ld    # 🌟 自定义链接脚本 (手动划定 Region 2 和 Region 3 的内存边界)
 │
 ├── 📁 1_App_Layer/                # 业务应用层 (运行在用户态 Unprivileged)
 │    ├── main.c                    # 系统入口，装载模块
 │    ├── app_fsm.c / .h            # 核心业务状态机
 │    └── app_usb_proto.c / .h      # USB 自定义通信协议解析器
 │
 ├── 📁 2_Security_Enclave/         # 安全中间件层 (运行在特权态 Privileged)
 │    ├── sec_crypto.c / .h         # AES-256-GCM 算法核心
 │    ├── sec_rng.c / .h            # 物理真随机数发生器 (利用 ADC 噪声)
 │    └── sec_pipeline.c / .h       # 零拷贝流式处理管线控制器
 │
 ├── 📁 3_Nano_OS_Core/             # 微内核基础层 (运行在特权态 Privileged)
 │    ├── os_core.c / .h            # 任务控制块(TCB)管理与任务创建
 │    ├── os_cpu_asm.s              # 纯汇编 PendSV 上下文切换与 SVC 系统调用
 │    └── os_sched.c / .h           # SysTick 节拍器与轮询调度逻辑
 │
 └── 📁 4_BSP_Drivers/              # 板级硬件驱动层 (裸金属操作)
      ├── bsp_mpu.c / .h            # 🌟 MPU 内存保护单元防火墙配置
      ├── bsp_usb.c / .h            # USB-OTG 控制器与 DMA 驱动
      ├── bsp_flash.c / .h          # W25Q128 SPI 轮询/DMA 读写驱动
      └── bsp_led_wdg.c / .h        # RGB 状态灯与硬件看门狗驱动

```

---

### 🧩 二、 核心模块内容、目的与最终实现结构

我们按照“自底向上”的视角来拆解四大核心模块：

#### 1. `4_BSP_Drivers` (硬件驱动模块)

* **包含内容**：STM32 底层寄存器封装，包括 MPU、USB、SPI、GPIO、看门狗。
* **实现目的**：对上层操作系统屏蔽所有 STM32 硬件细节。上层代码只需要调用 `Flash_Write()`，而不需要知道底层是 SPI 还是 I2C。
* **最终实现结构**：全部为 C 语言编写的无状态函数库。**禁止**在此层包含任何业务逻辑或阻塞式死循环（如 `while(1)` 等待）。

#### 2. `3_Nano_OS_Core` (微内核模块)

* **包含内容**：TCB 结构体、`PendSV` 汇编调度器、`SysTick` 时钟管理。
* **实现目的**：提供多任务并发能力，管理 CPU 的执行权。它是整个系统的心脏起搏器。
* **最终实现结构**：维护一个 `TCB_t tasks[3]` 的静态数组。对外提供 `OS_Init()`, `OS_TaskCreate()`, `OS_Start()` 和 `OS_Yield()` 四个标准 API。

#### 3. `2_Security_Enclave` (安全飞地模块)

* **包含内容**：AES 加密算法库、密钥生命周期管理、零拷贝数据流缓冲指针。
* **实现目的**：确保所有敏感运算在 MPU 划定的 `Region 3` 内完成。
* **最终实现结构**：代码中关键的密钥数组和加密上下文变量，必须使用 `__attribute__((section(".enclave_data")))` 宏进行标记，指示链接器将其存放在安全的物理内存段中。对外仅提供特权接口（需通过 SVC 软中断调用）。

#### 4. `1_App_Layer` (应用状态机模块)

* **包含内容**：一个贯穿全局的 `switch-case` 状态机，以及 USB 报文的校验逻辑。
* **实现目的**：实现“设备插入 -> 握手 -> 取数 -> 加密 -> 断开”的全自动盲盒业务闭环。
* **最终实现结构**：作为一个常驻任务（Task）运行在 Nano-OS 上。通过事件标志位（如 `FLAG_USB_RX_DONE`）驱动状态流转，并根据状态控制 LED 的红绿蓝展示。

---

### 🕸️ 三、 模块依赖关系 (Dependency Graph)

高可用系统的核心法则是“单向依赖，严禁循环调用”。

* **[App_Layer]** ---> 依赖 `Nano_OS_Core` (用于交出执行权) + 依赖 `Security_Enclave` (发送取数指令) + 依赖 `BSP` (控灯)。
* **[Security_Enclave]** ---> 依赖 `BSP` (读写 Flash，操作 USB 数据缓冲)。
* **[Nano_OS_Core]** ---> 依赖 `BSP` (SysTick 定时器硬件配置)。
* **[BSP_Drivers]** ---> **底层，不依赖任何上层模块。**

> ⚠️ **安全架构强制约束**：`App_Layer` 运行在用户态，**绝对禁止**直接 `#include "sec_crypto.h"` 去调用底层加密函数，必须通过 `OS_Core` 提供的系统调用（SVC）陷入特权态后才能执行加密。

---

### 🎯 四、 渐进式测试验收标准 (Testing Strategy)

要让系统不崩溃，必须按照以下 4 个阶段进行严格的独立测试。这就是你开发时的“打怪升级路线”：

#### Stage 1: 硬件驱动验收 (裸机测试，不跑 OS)

* **测试动作**：在 `main()` 函数里直接写测试代码。
* **期望现象**：
1. RGB 灯能分别亮出红、绿、蓝纯色。
2. 向 W25Q128 的 `0x000000` 地址写入一串字符串 "Hello_SecuFerry"，断电重启后，能成功将其读出并通过串口或调试器查看。


* **验收通过**：底层 BSP 完全健康，硬件焊接无虚焊。

#### Stage 2: 微内核与上下文切换验收

* **测试动作**：实例化两个任务 `Task_Red` 和 `Task_Blue`。`Task_Red` 点亮红灯，然后执行 `OS_Yield()`；`Task_Blue` 点亮蓝灯，然后执行 `OS_Yield()`。
* **期望现象**：肉眼看到板子上的 LED 呈现紫色（红蓝极速交替闪烁），或者降速后看到红蓝交替。
* **验收通过**：你手写的 `PendSV` 汇编寄存器压栈出栈逻辑完美无缺，CPU 没有跑飞。

#### Stage 3: MPU 护城墙防御验收 (高分项，必须录视频留证)

* **测试动作**：激活 MPU，将 `0x2001C000` 设为特权访问。在处于用户态的 `Task_USB` 中，故意写一行代码：`uint32_t steal_key = *(uint32_t*)0x2001C000;`。
* **期望现象**：代码执行到这一行的瞬间，系统立刻触发硬件 `MemManage_Handler` 异常，随后状态机捕获异常，LED 变成**红色爆闪**并死锁。
* **验收通过**：内存物理隔离墙搭建成功，缓冲区溢出攻击彻底失效。

#### Stage 4: 联调与状态机闭环 (系统级集成测试)

* **测试动作**：
1. 电脑端写一个简单的 Python 脚本，模拟工控机，向 USB 设备发送假日志数据。
2. 将设备插入电脑，观察 LED 状态。


* **期望现象**：
1. 插入电脑 -> **蓝灯慢闪**（握手）。
2. 运行 Python 脚本发送数据 -> 设备**蓝绿交替狂闪**（流式加密吸入）。
3. 数据发送完毕 -> 设备**绿灯常亮**，且在电脑的设备管理器中，该 USB 设备**自动消失**（物理断链）。
4. 拔下设备，通过调试器强行读取 W25Q128 数据，发现全是乱码（已 AES 加密）。


* **验收通过**：恭喜，SecuFerry-OS V1.0 可以直接拉去参赛答辩了！