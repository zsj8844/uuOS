
+------------------+          +------------------+
|     工控网        |          |      密网         |
|                  |          |                  |
| PLC <-> 工控网关  |          | 调度程序+明文存储 |
| (持有 MK_DATA)   |          | (持有 MK_CTRL)   |
+--------+---------+          +--------+---------+
         |                             |
         | USB Host                    | USB Host
         |                             |
         +-------+       +-------------+
                 |       |
          (物理开关分时连接)
                 |       |
         +-------+-------+
         |   STM32 单USB  |
         |   Device CDC   |
         +-------+-------+
                 |
    +------------+------------+
    |                         |
+---+---+ 域识别模块  +-----+-----+
| 工控侧 |<--------->| 密网侧     |
| 鉴权   | 选择密钥  | 鉴权       |
| MK_DATA|           | MK_CTRL    |
| 生成SK2|           | 生成SK1    |
+---+---+           +-----+-----+
    |                     |
    | 暂存加密数据         | 接收任务 / 授权解密
    v                     v
+---+------------+  +----+----+------------+
| 密文暂存区     |  | 任务存储 | 解密模块   |
| (内层密文不解) |  | (取数任务) | MK_DATA  |
+----------------+  +---------+------------+
                         |         |
                         +----+----+
                              |
                              v
                    明文 -> SK1加密 -> USB回传


```mermaid
flowchart TB
    subgraph 工控网
        GW[工控网关<br/>持有 MK_DATA]
    end

    subgraph 密网
        DISP[调度程序 + 明文存储<br/>持有 MK_CTRL]
    end

    subgraph STM32 [STM32 单 USB Device CDC]
        direction TB
        DOM[域识别模块]

        subgraph 工控侧
            AUTH1[工控侧鉴权<br/>使用 MK_DATA]
            SK2[生成 SK2]
            TEMP[密文暂存区<br/>内层密文不解]
        end

        subgraph 密网侧
            AUTH2[密网侧鉴权<br/>使用 MK_CTRL]
            SK1[生成 SK1]
            TASK[任务存储<br/>取数任务]
            DEC[解密模块<br/>持有 MK_DATA]
        end

        SEC[安全存储<br/>MK_DATA / MK_CTRL]
    end

    %% 物理分时连接（USB Host）
    GW -- USB Host --> DOM
    DISP -- USB Host --> DOM

    %% 域识别后选择侧
    DOM -- 0x01 密网 --> AUTH2
    DOM -- 0x02 工控 --> AUTH1

    %% 密钥访问
    SEC -- MK_DATA --> AUTH1
    SEC -- MK_CTRL --> AUTH2
    SEC -- MK_DATA --> DEC

    %% 工控侧流程
    AUTH1 --> SK2
    SK2 -- 接收加密数据 --> TEMP

    %% 密网侧流程
    AUTH2 --> SK1
    SK1 -- 下发任务/授权解密 --> TASK
    TASK -- 任务信息 --> DEC
    TEMP -- 内层密文 --> DEC
    DEC -- 明文 → SK1 加密 --> DISP

    %% 附加交互：任务存储写入暂存区关联（可选）
    TASK -.-> TEMP
```


                  
# 安全存储区（位于 STM32 内部，仅各模块可访问）

- **MK_DATA**：工控数据加解密密钥，与工控网关共享。
- **MK_CTRL**：密网控制鉴权密钥，与密网调度程序共享。

---

## 各模块职责与交互

| 模块 | 位置 | 功能 |
|------|------|------|
| 域识别模块 | STM32 USB 驱动层 | 解析握手 Hello 帧首个字节的 Domain 字段（0x01=密网，0x02=工控），据此激活对应侧的鉴权模块，并通知安全存储开放对应密钥访问。 |
| 工控侧鉴权模块 | STM32 内部 | 使用 MK_DATA 与工控网关执行完整的 ECDHE 双向认证，生成会话密钥 SK2。认证通过后，后续的工控数据接收将由该模块解密外层（SK2 层）。 |
| 密网侧鉴权模块 | STM32 内部 | 使用 MK_CTRL 与密网调度程序执行 ECDHE 双向认证，生成会话密钥 SK1。负责所有密网指令的解密与响应的加密，并控制解密模块的“使能”信号。 |
| 任务存储区 | STM32 非易失性存储 | 保存密网下发的取数任务（例如：目标 PLC 地址、数据点列表、采集周期等），任务可持久化，断电不丢失。任务更新时由密网鉴权模块解密后写入。 |
| 密文暂存区 | STM32 RAM/Flash | 存放从工控网关接收的“内层密文”（由 MK_DATA 加密），并关联任务 ID。该区域数据不解密，仅做临时仓储。 |
| 解密模块 | STM32 内部 | 持有 MK_DATA，但解密动作受密网鉴权模块控制。当且仅当密网通过 SK1 下发合法解密指令，该模块从暂存区取出对应内层密文，用 MK_DATA 解密得到明文，并将明文交给密网鉴权模块加密回传。 |
| 安全存储 | STM32 写保护区域 | 存储 MK_DATA 和 MK_CTRL，只在各自鉴权模块或解密模块内部使用，不可被其他代码读取。 |    

# 二、流程图详解（三阶段交互序列）
## 阶段 0：密网下发任务（第一次连接）


```mermaid
sequenceDiagram
    participant 调度 as 密网调度 (USB Host)
    participant STM32 as STM32 (USB Device)

    调度->>STM32: USB连接, Hello (Domain=0x01)
    Note over STM32: 域识别 → 激活密网侧鉴权，开放 MK_CTRL

    loop 双向认证 (ECDHE + HMAC)
        STM32->>调度: 交换随机数、临时公钥
        调度->>STM32: 基于 MK_CTRL 计算 HMAC 签名
        Note over 调度,STM32: 验证通过后各自计算共享密钥，用 HKDF 派生 SK1
    end

    调度->>STM32: 下发取数任务 (SK1 加密，AES-128-GCM)
    Note over STM32: 解密并验证完整性，写入任务存储区（持久化）
    STM32-->>调度: 任务确认 (AES-GCM(SK1, "ACK"))

    调度->>STM32: 断开 USB
    Note over STM32: 销毁 SK1，任务持久保留
```
密网调度(USB Host)          STM32(USB Device)
       |                         |
       |-- USB连接，Hello ----->|  (Domain=0x01)
       |                         |  域识别 -> 激活密网侧鉴权，开放 MK_CTRL
       |<--- 握手鉴权 ---------->|  使用 MK_CTRL 的 ECDHE 双向认证
       |         生成 SK1         |
       |                         |
       |-- 下发取数任务 ------->|  任务用 SK1 加密（AES-128-GCM）
       |   (任务包含：目标PLC,    |  解密后验证完整性，写入任务存储区
       |    数据点, 采集策略)     |
       |                         |
       |<-- 任务确认 -----------|  AES-GCM(SK1, "ACK")
       |         断开 USB        |  销毁 SK1，任务持久保留

## 详解：密网通信流程

### 1. 域识别

- Hello 帧第一个字节为 `0x01`，域识别模块将其识别为**密网**。
- 随后激活密网侧鉴权模块，并通知安全存储开放 `MK_CTRL` 密钥访问。

### 2. 鉴权过程（基于 MK_CTRL 的双向 ECDHE 认证）

1. **交换随机数与临时公钥**  
   双方交换随机数（nonce）和临时 Curve25519 公钥。
2. **HMAC 签名验证**  
   分别基于 `MK_CTRL` 计算 HMAC 签名并互相验证。
3. **生成会话密钥 SK1**  
   验证通过后，各自计算共享密钥，并用 HKDF 派生会话密钥 `SK1`。

### 3. 任务下发与存储

- 任务下发指令由 `SK1` 加密保护。
- STM32 解密后得到任务结构体（包含目标 PLC 地址、数据点列表、采集周期等）。
- 任务写入**非易失性任务存储区**，**断电不丢失**。

### 4. 会话清理

- 断开连接前销毁 `SK1`，保证会话隔离。       

# 阶段 1：工控采集加密（切换至工控侧）
```mermaid
sequenceDiagram
    participant 网关 as 工控网关 (USB Host)
    participant STM32 as STM32 (USB Device)

    网关->>STM32: USB连接, Hello (Domain=0x02)
    Note over STM32: 域识别 → 激活工控侧鉴权，开放 MK_DATA

    loop 双向认证 (ECDHE + HMAC)
        STM32->>网关: 交换随机数、临时公钥
        网关->>STM32: 基于 MK_DATA 计算 HMAC 签名
        Note over 网关,STM32: 验证通过后各自计算共享密钥，用 HKDF 派生 SK2
    end

    Note over STM32: 根据任务存储区内容，构造数据请求帧
    STM32->>网关: 数据请求 (数据点列表等，SK2 加密)

    Note over 网关: 从 PLC 采集原始数据 raw<br/>inner = AES-GCM(MK_DATA, raw)<br/>outer = AES-GCM(SK2, inner)
    网关->>STM32: outer (双层密文)
    Note over STM32: 用 SK2 解密外层得到 inner<br/>将 inner 与任务 ID 关联存入密文暂存区

    网关->>STM32: 断开 USB
    Note over STM32: 销毁 SK2
```



工控网关(USB Host)          STM32(USB Device)
       |                         |
       |-- USB连接，Hello ----->|  (Domain=0x02)
       |                         |  域识别 -> 激活工控侧鉴权，开放 MK_DATA
       |<--- 握手鉴权 ---------->|  使用 MK_DATA 的 ECDHE 双向认证
       |         生成 SK2         |
       |                         |
       |   STM32 可主动请求数据   |  根据任务存储区内容，构造数据请求帧
       |   (数据点列表等)         |  用 SK2 加密发送给网关
       |                         |
       |   网关采集 PLC 数据      |
       |   inner = AES-GCM(MK_DATA, raw)
       |   outer = AES-GCM(SK2, inner)
       |<-- outer (双层密文) ---|  接收外层，用 SK2 解密得到 inner
       |                         |  将 inner 与任务 ID 关联存入密文暂存区
       |         断开 USB        |  销毁 SK2
## 详解：工控侧通信流程（Domain=0x02）

### 1. 域识别与密钥使用

- Hello 帧第一个字节为 `0x02`，域识别模块将其识别为**工控**。
- 激活工控侧鉴权模块，鉴权使用 `MK_DATA`，生成会话密钥 `SK2`。

### 2. 任务驱动的数据请求

- STM32 读取预存任务（来自非易失性任务存储区）。
- 根据任务中的**目标 PLC 地址**和**数据点列表**，STM32 主动向工控网关发送数据请求。
  > 注：也可设计为网关按策略推送，但主动请求更符合任务驱动模型。

### 3. 网关处理与双层加密

网关收到请求后，从 PLC 读取原始数据 `raw`，执行双层加密：

1. **内层加密**：用 `MK_DATA` 加密 `raw`，得到 `inner`（内层密文）。
2. **外层加密**：用 `SK2` 加密 `inner`，得到 `outer`（传输密文）。

> 双层加密确保：即使传输链路被监听，没有 `MK_DATA` 也无法解出原始数据。

### 4. STM32 接收与暂存

- STM32 使用 `SK2` 解密外层，得到 `inner`（不解密内层）。
- 将 `inner` 与对应的**任务 ID**、**采集时间戳**一起存入**密文暂存区**。
- 暂存区可容纳多个密文记录。

### 5. 会话清理

- 断开 USB 连接后，立即销毁 `SK2`，保证会话隔离。

# 阶段 2：密网授权解密（再次连接密网）

```mermaid
sequenceDiagram
    participant 调度 as 密网调度 (USB Host)
    participant STM32 as STM32 (USB Device)

    调度->>STM32: USB连接, Hello (Domain=0x01)
    Note over STM32: 域识别 → 激活密网侧鉴权，开放 MK_CTRL

    loop 双向认证 (ECDHE + HMAC)
        STM32->>调度: 交换随机数、临时公钥
        调度->>STM32: 基于 MK_CTRL 计算 HMAC 签名
        Note over 调度,STM32: 验证通过后各自计算共享密钥，用 HKDF 派生新 SK1'
    end

    opt 可选：查询待取数据
        调度->>STM32: 查询待取数据？
        STM32-->>调度: 密文ID列表 (用 SK1' 加密)
    end

    调度->>STM32: 下发解密指令 (含目标密文ID，用 SK1' 加密)
    Note over STM32: 验证指令，从暂存区取出对应 inner<br/>raw = AES-GCM-Dec(MK_DATA, inner)
    STM32-->>调度: AES-GCM(SK1', raw)  (明文立即加密回传)
    Note over 调度: 解密得到明文，写入存储

    调度->>STM32: 断开 USB
    Note over STM32: 销毁 SK1'，可清除已解密的密文
```

密网调度(USB Host)          STM32(USB Device)
       |                         |
       |-- USB连接，Hello ----->|  (Domain=0x01)
       |<--- 握手鉴权 ---------->|  使用 MK_CTRL 生成新 SK1'
       |                         |
       |-- 查询待取数据？--------|  可选步骤：STM32 上报暂存区密文ID列表
       |<-- 密文ID列表(加密) ---|  列表用 SK1' 加密回传
       |                         |
       |-- 下发解密指令 ------->|  指令含目标密文ID，用 SK1' 加密
       |                         |  验证指令，从暂存区取出对应 inner
       |                         |  解密使能：raw = AES-GCM-Dec(MK_DATA, inner)
       |                         |
       |<-- AES-GCM(SK1', raw) -|  明文立即用 SK1' 加密回传
       |  解密得明文，写入存储   |
       |         断开 USB        |  销毁 SK1'，可清除已解密密文

       ## 详解：密网域再次连接（第 N 次）

### 1. 重新握手与密钥派生

- 再次连接时，仍为密网域（`Domain=0x01`）。
- 握手生成全新的会话密钥 `SK1'`，**与前次 SK1 无关**（会话完全隔离）。

### 2. 可选查询步骤

- STM32 可**上报暂存区元数据**：将暂存区中所有密文的 ID 和元数据（如任务来源、采集时间戳）用 `SK1'` 加密上报。
- 操作员可选择需要解密的数据，也可自动按任务批量解密。

### 3. 解密指令验证

- 解密指令由 `SK1'` 加密保护。
- STM32 解密后校验**序列号**，防止重放攻击。
- 验证通过后，**临时使能解密模块**。

### 4. 解密内层密文

- 从密文暂存区取出对应的 `inner`（内层密文）。
- 调用硬件 AES-GCM，使用 `MK_DATA` 解密，得到原始明文 `raw`。

### 5. 加密回传与清理

- `raw` 在内存中明文存在时间**极短**，立即用 `SK1'` 加密回传。
- 已解密的暂存区条目可标记清除（或等待后续批量清理）。

### 6. 会话结束

- 断开 USB 连接后销毁 `SK1'`，完成一次完整的数据获取与解密流程。