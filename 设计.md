1. **外形类似U盘，接口不限于USB**
   物理形态小巧，可选用USB、Type-C、甚至以太网口或串口，取决于目标工控机接口。

2. **在"密码系统"端设定任务**
   任务描述（如：从工控机的某路径/某寄存器/某OPC点读取数据）由管理员预先配置。

3. **插入工控网后主动获取数据**
   设备内部有OS/单片机固件，能自动执行任务脚本，无需人工在工控机上操作。

4. **设备本身加密**
   存储数据加密（如AES-256），防止物理拆解后直接读取；也可加入防篡改设计。

5. **回到密码系统核实结果**
   设备返回后，由密码系统解密并校验数据完整性，确认任务成功。

6. **避免非安全监控下的人为拷贝风险**
   全流程自动化，不依赖操作员鼠标点击或拖拽文件，杜绝中间人篡改或窃取。

7. **仅支持这一种工作，简单好写**
   专注单一功能，降低开发复杂度和攻击面。
  
物理硬件功能：
STM32F405 最小系统板（核心主控）
这是你整个设备的“大脑”，你的操作系统、加密算法和工控协议都会烧录在这里面。

物理特征：一块大拇指大小的长条形黑色或蓝色电路板，中间有一颗正方形的黑色 STM32 芯片，一端自带 Type-C 或 Micro-USB 接口，两侧有排针。

购买认准：搜索“STM32F405 核心板”或“STM32F405 黑丸板”。

2️⃣ W25Q128 模块（外部加密存储器）
用来当作设备的“加密保险箱”。工控机上抓来的数据，被核心板加密后，就存在这颗芯片里。

物理特征：一块非常小的正方形小板子（通常只有指甲盖大），上面有一颗 8 个引脚的黑色微型芯片，旁边印着 W25Q128 字样。

购买认准：搜索“W25Q128 SPI Flash 模块”。

3️⃣ W5500 以太网模块（网络接口）
用来连接工控机的网口。通过它，你的系统可以自动向工控机发送网络请求（如 Modbus-TCP 协议）并抓取数据。

物理特征：一块带有标准 RJ45 网线接口（就是插网线的那个大方口）的电路板，上面有一颗 W5500 芯片和一些电容电阻。

购买认准：搜索“W5500 模块”或“SPI 转网口模块”。

4️⃣ TTL 转 RS485 模块（工业串口接口）
工业现场最常用的通信接口之一。用来连接那些带有 RS485 串口的工业设备。

物理特征：一块小板子，一端是排针（接STM32），另一端通常是一个绿色的接线端子（用螺丝拧紧导线的那种），用来接工业设备的 A、B 信号线。

购买认准：搜索“TTL转RS485 自动流控模块”（建议买带自动流控的，写驱动时最省心）。

5️⃣ ST-Link V2 烧录器（内核调试器）
它是连接电脑和 STM32 的桥梁。你在电脑上写好的操作系统代码，必须通过它才能“烧录”到 STM32 芯片里，调试代码、找 Bug 全靠它。

物理特征：外形长得非常像一个长条形的铝合金 U 盘（通常有金属红、蓝、黑等颜色），一端是 USB 口插电脑，另一端露出一排排针，用杜邦线连到 STM32 板子上。

购买认准：搜索“ST-Link V2 仿真器”或“STLINK 迷你下载器”。

6️⃣ RGB LED 颗粒（状态指示灯）
用来实现你的“无头/盲盒模式”。因为设备没有屏幕，系统运行到哪一步（如：蓝灯表示正在抓取数据，绿灯表示加密成功，红灯表示校验失败），全靠这颗灯的颜色和闪烁频率来告诉操作员。

物理特征：一个带有 4 个引脚的小灯泡颗粒（有直插式的也有贴片模块形式的），能通过程序控制同时发出红、绿、蓝三种颜色的光。

购买认准：搜索“RGB LED 模块”或“三色 LED 灯”。






物理硬件：
STM32F405 最小系统板（核心主控）
W25Q128 模块（外部加密存储器）
ST-Link V2 烧录器（内核调试器）
RGB LED 颗粒（状态指示灯）

暂时不开发：
//W5500 以太网模块（网络接口）
//TTL 转 RS485 模块（工业串口接口）

