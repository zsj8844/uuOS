这是为你优化修改后的**项目设计与硬件清单描述**。我针对你最终选定的 **STM32F411CEU6** 核心板进行了适配，并对“暂不开发网口/串口”后的 **USB 自动化数据流逻辑** 进行了合规化修正，使其更符合内核比赛的官方文档规范（突出了 USB 协议栈、MPU 隔离和自动化盲盒取数设计）。

你可以直接复制这段话作为你的项目申报书或设计方案初稿：

---

# 🏆 SecuFerry-OS 项目设计方案与硬件清单

## 一、 系统核心功能特性

1. **外形类似U盘，物理形态小巧**
设备外形如大拇指大小，一阶段原生采用 **Type-C / USB 接口** 直接接入目标工控上位机。硬件引脚和内核驱动层留有扩展性，二期可无缝升级以太网口或工业串口。
2. **在"密码系统"端设定任务**
任务描述（如：从工控机的某路径读取关键日志、或特定内存段数据）由管理员在安全的“密码系统”端预先配置，并加密写入设备的存储芯片中。
3. **插入工控机后主动、全自动获取数据**
设备内部运行自研微内核操作系统。插入目标工控机后，无需人工在工控机上进行任何鼠标点击或文件拖拽操作，内核自动通过 **USB Custom HID 协议** 与工控机后台的代理脚本进行安全握手，并主动将指定数据“吸入”设备。
4. **设备内核级加密与防拆解**
所有吸入的数据在内核态的“安全飞地（Enclave）”内存区内，使用高强度算法（如 AES-256）进行实时流式加密后再落盘存储。即使设备被物理拆解、Flash 芯片被取下，外界也无法解密读取任何明文数据。
5. **回到密码系统核实结果**
任务完成后，设备被带回安全区域插入密码系统。由密码系统读取密文、解密并校验数据完整性（如使用 SHA-256 或 GCM 标签），最终确认数据摆渡任务成功。
6. **杜绝非安全监控下的人为拷贝与泄密风险**
全流程数据流由自研内核状态机刚性控制，不挂载任何标准 USB 大容量存储（MSC）协议（工控机不会将其识别为普通 U 盘），彻底杜绝了操作员私自拷贝、中间人篡改或工控勒索病毒感染的风险。
7. **专注单一安全功能，降低攻击面**
系统摒弃了通用操作系统的冗余组件，剥离了动态内存分配和复杂文件系统，专注于“数据安全摆渡”这一单一任务，极大缩小了被外部漏洞利用的攻击面。

---

## 二、 物理硬件功能清单（一期开发）

### 1️⃣ STM32F411CEU6 最小系统板（核心主控）

* **系统职能**：这是整个摆渡设备的“数字大脑”，自研微内核操作系统、轻量级加密算法、自定义 USB HID 协议栈以及核心状态机都会烧录在其中。利用其内置的 **MPU（内存保护单元）**，在硬件层实现内核态与用户态的内存安全隔离。
* **物理特征**：一块大拇指大小的长条形黑色电路板（俗称 F411 黑神板），中间有一颗正方形黑色 STM32F411 芯片，一端自带 Type-C 接口，两侧预留排针用于后期功能扩展。
* **购买认准**：搜索“STM32F411CEU6 核心板”或“WeAct F411 黑神板”。

### 2️⃣ W25Q128 模块（外部加密存储器）

* **系统职能**：用来当作设备的“数据加密保险箱”。工控机上位机传输过来的明文数据，被 F411 主控核心板实时加密成密文后，通过 SPI 总线源源不断地写入这颗芯片中。
* **物理特征**：一块非常小的正方形小板子（指甲盖大小），上面有一颗 8 个引脚的黑色微型芯片，旁边印着 W25Q128 字样（容量为 16MB/128M-bit）。
* **购买认准**：搜索“W25Q128 SPI Flash 模块”。

### 3️⃣ ST-Link V2 烧录器（内核调试器）

* **系统职能**：连接开发电脑与 STM32F411 核心板的底层硬件桥梁。在电脑上编写的汇编上下文切换代码和内核 C 语言代码，必须通过它烧录进芯片，并利用它进行内核级的断点调试（SWD 模式）。
* **物理特征**：外形类似长条形铝合金 U 盘，一端为标准 USB 口插电脑，另一端露出一排排针，通过杜邦线与主控板的 SWD 接口相连。
* **购买认准**：搜索“ST-Link V2 仿真器”或“STLINK 迷你下载器”。

### 4️⃣ RGB LED 颗粒（状态指示灯）

* **系统职能**：实现设备的“无头/盲盒运行模式”。由于高安全物理形制要求设备不带任何屏幕，内核状态机的流转全靠这颗灯。例如：蓝灯慢闪表示等待工控机握手，蓝灯快闪表示正在吸入并加密数据，绿灯常亮表示任务成功封仓，红灯爆闪表示触发内存越界或校验失败异常并启动内核自毁死锁。
* **物理特征**：带有 4 个引脚的小灯泡颗粒或微型模块，能通过内核的 GPIO / PWM 程序控制，同时调制出红、绿、蓝三种颜色的光。
* **购买认准**：搜索“RGB LED 模块”或“三色 LED 灯”。

---

## 三、 预留功能模块（二期技术演进）

以下工业级级物理接口模块在当前阶段**暂不投入代码开发**，但硬件引脚已做预留，后续可无缝升级：

* **// W5500 以太网模块（网络接口）**：用于直接插入不带 USB 口的工业交换机或网络自动化设备，通过 Modbus-TCP 协议主动抓取网络层数据。
* **// TTL 转 RS485 模块（工业串口接口）**：用于直接连接现场的 PLC、变频器或传统温控表等带有 RS485 串口的工业底层硬件，通过 Modbus-RTU 协议读取寄存器数据。



物理硬件：
STM32F411CEU6 STM32F4  最小系统板（核心主控）
W25Q128 模块（外部加密存储器）
ST-Link V2 烧录器（内核调试器）
RGB LED 颗粒（状态指示灯）
面包板


暂时不开发：
//W5500 以太网模块（网络接口）
//TTL 转 RS485 模块（工业串口接口）

